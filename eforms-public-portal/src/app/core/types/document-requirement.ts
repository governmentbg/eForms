export const documentRequirement = {
    required: 'required', //document is reuired
    optional: 'optional', //user chooses if the document will be submited
    nonRequired: 'non-required', //documenet isn't required
    requiredFromMain: 'required-from-main', //document is required because of data in main document( основно заявление)
    nonRequiredFromMain: 'non-required-from-main', //document not is required because of data in main document( основно заявление)
    userChoiceRequired:'required-from-user-choice', //document is required based on user choice from radio buttons
    userChoiceNonRequired:'non-required-from-user-choice', //document not is required based on user choice from radio buttons
}
